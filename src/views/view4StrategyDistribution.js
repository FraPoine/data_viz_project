import { registerViewPlaceholder } from "../components/viewShell.js";
export function initView4(container) {
  return registerViewPlaceholder(container, "View 4 — First Entries, Extensions, and Remakes");
}
