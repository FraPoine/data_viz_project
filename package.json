{
  "name": "disney-dreamworks-visual-story",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deliverable 3 visual-story implementation skeleton for Data Visualization Lab.",
  "scripts": {
    "data:check": "python3 scripts/validate_frozen_inputs.py",
    "validate:skeleton": "node scripts/validate-skeleton.mjs",
    "validate": "npm run data:check && npm run validate:skeleton",
    "dev": "node scripts/serve.mjs source 5173",
    "build": "npm run validate && node scripts/build.mjs",
    "preview": "node scripts/serve.mjs dist 4173"
  },
  "engines": {
    "node": ">=20"
  }
}
